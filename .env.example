# PageRank Configuration
# Copy this file to .env and adjust values

# Input graph file (edge list format: "from to" per line)
INPUT_FILE=./Wiki-Vote.txt

# Number of PageRank iterations (default: 80)
NUM_ITERATIONS=80

# MPI configuration
NUM_MPI_PROCESSES=4

# OpenMP threads per MPI process
NUM_THREADS=4

# Damping factor (default: 0.85)
DAMPING_FACTOR=0.85

# Convergence tolerance (for future use)
TOLERANCE=0.0000001
